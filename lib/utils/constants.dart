String documentIdFromLocalData() => DateTime.now().toIso8601String();

class AppConstants {
  AppConstants._();
  static const String pending = "pending";
  static double appHeight = 825;
  static double appWidth = 393;
}
